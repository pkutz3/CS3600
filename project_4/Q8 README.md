Q8 README

To run the training code and output its accuracies, simply uncomment 'q8()' in Testing.py and input how many sessions and hidden layers are wanted. Then run Testing.py. The data will already have been initialized through getNNExtraData() and buildExamplesFromExtraData().